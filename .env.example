# NRC Management System - Environment Configuration
# Copy this file to .env.local and update values for your environment

# ============================================
# SERVER CONFIGURATION
# ============================================

# Server Port
PORT=3001

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Node Environment (development, production, test)
NODE_ENV=development

# ============================================
# JWT CONFIGURATION
# ============================================

# JWT Secret Key - IMPORTANT: Change this in production!
# Generate a secure secret: openssl rand -base64 32
# For production on Vercel, set this as a secret environment variable
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Expiration (format: "24h", "7d", seconds as number, etc.)
JWT_EXPIRES_IN=24h

# ============================================
# API CONFIGURATION
# ============================================

# API Base URL for frontend (used by Vite)
VITE_API_URL=http://localhost:3001/api

# ============================================
# DATABASE CONFIGURATION
# ============================================

# For CSV-based storage (default)
# Data files are stored in public/data/ directory
DATA_DIR=public/data

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================

# When deploying to Vercel:
# 1. Set JWT_SECRET to a secure random string (use 'openssl rand -base64 32' to generate)
# 2. Set FRONTEND_URL to your production frontend URL
# 3. Set VITE_API_URL to your production API URL
# 4. Ensure NODE_ENV is set to 'production'
# 5. Add these variables in Vercel Settings > Environment Variables

# Example for production:
# PORT=3001
# FRONTEND_URL=https://yourdomain.com
# VITE_API_URL=https://api.yourdomain.com/api
# NODE_ENV=production
# JWT_SECRET=<use-vercel-secrets-for-this>
# JWT_EXPIRES_IN=24h
